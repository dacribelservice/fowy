# Hoja de Ruta - 3 de Junio

### Fase 1: Gestión de Negocios Inactivos / Pausados

- [x] **1.1. Ocultar negocios inactivos del mapa**: Modificar `src/hooks/useExplorerManager.ts` para que la función `fetchBusinesses` filtre los resultados asegurando que solo se muestren los negocios activos (`status === true`) que además estén abiertos (`isBusinessOpen(biz.schedules)`).
- [x] **1.2. Pantalla Glassmorphism en Menú Inactivo**: Editar `src/app/(explorer)/[slug]/page.tsx` para verificar el estado del negocio (`const isBusinessActive = business?.status === true;`). Si el negocio está inactivo, renderizar un overlay absoluto con efecto de vidrio esmerilado premium (`backdrop-blur-md`) que bloquee la interacción con el menú.
- [x] **1.3. Diseño de la Pantalla de Mantenimiento**: Diseñar una tarjeta central sobre el overlay que contenga un mensaje de "Menú en mantenimiento" y un botón de "Regresar al mapa". (importante los logos deben de ser minimalistas) ⚠️

### Fase 2: Corrección de Métricas y Gráficas de Ventas

- [x] **2.1. Corregir contador de Pedidos Recibidos y Ticket Promedio**: Modificar `src/components/admin/businesses/BusinessMetricsList.tsx` para usar la consulta de conteo nativo de Supabase (`count: "exact"`) para la cantidad de pedidos y, preferiblemente, implementar una función RPC (ej. `get_business_sales_stats`) para calcular el ticket promedio sin descargar la tabla completa de pedidos (lo cual causa que el total se quede estancado en 1000 debido al límite de la base de datos).
- [x] **2.2. Solucionar días vacíos ($0) en la Gráfica de Ventas**: Modificar el hook `src/components/admin/businesses/useFowySalesData.ts`. En la consulta principal (`supabase.from("orders")...`), calcular la fecha de inicio del período en el cliente (ej. 7 días, 6 semanas o 6 meses atrás) y añadir un filtro `.gte("created_at", fechaInicio)`. Esto evita descargar los 1000 registros más antiguos y garantiza que la gráfica siempre reciba los datos de los días recientes.

### Fase 3: Gestión de Menú del Negocio desde el Panel de Administrador

- [x] **3.1. Agregar Botón de "Tienda" en la Tabla (BusinessList)**: Modificar la tabla de listado (`src/components/admin/businesses/BusinessList.tsx`) añadiendo un botón con el ícono de "Tienda" junto al botón de "Lápiz". Este nuevo botón enlazará a la ruta exclusiva: `/admin/negocios/[id]/catalogo`.
- [x] **3.2. Crear Nueva Pantalla Completa (Ruta de Catálogo)**: Crear un archivo totalmente nuevo en `src/app/admin/negocios/[id]/catalogo/page.tsx` para alojar la gestión del menú. Esto brindará todo el ancho de pantalla necesario y separará el contexto de configuración general (pagos, módulos) del contexto de inventario.
- [x] **3.3. Ensamblar el "Remolque" (Reutilización Segura)**: Construir la nueva pantalla importando los subcomponentes y ganchos del socio (`ProductFormModal`, `useProductManager`, etc.) pasándoles el `businessId` de la URL. **Prohibido tocar/modificar** la vista actual del dueño (`src/app/(partners)/business/menu/page.tsx`) cumpliendo estrictamente con la Regla del Remolque.
- [x] **3.4. Validar Políticas RLS de Supabase**: Asegurar que las políticas de seguridad (RLS) en Supabase permitan al Super Administrador insertar, actualizar y eliminar registros en las tablas de `products`, `product_categories` y `categories` para cualquier `business_id`.

### Fase 4: Solución de Redirección a WhatsApp en iOS (Safari / WebViews)

- [x] **4.1. Eliminar `await` en la inserción de base de datos**: Modificar el hook `src/components/explorer/hooks/useCheckoutLogic.ts` en `handleSendWhatsApp` para disparar la inserción en Supabase en segundo plano sin usar `await`. Esto preserva el hilo de ejecución síncrono para el evento de clic.
- [x] **4.2. Cambiar redirección a `window.location.href`**: Reemplazar `window.open(whatsappUrl, "_blank")` por `window.location.href = whatsappUrl` en `useCheckoutLogic.ts`. Esto evita que iOS Safari bloquee el popup e invoque directamente la aplicación nativa de WhatsApp.
- [x] **4.3. Pruebas de compatibilidad**: Validar el flujo tanto en iPhone (Safari y WebViews) como en Android para confirmar que la redirección sea instantánea y que el pedido continúe registrándose correctamente en Supabase en segundo plano.

### Fase 5: Motor Central de Marketing y Banners Segmentados

#### 🗄️ subfase 5.1: Base de Datos, Estandarización y Migración SQL (Supabase)
- [ ] **5.1.1. Ejecutar Migración de Columnas en Supabase**: Ejecutar un script SQL en la consola de Supabase para añadir tres columnas nullable a `marketing_banners`: `target_city` (text), `target_business_id` (uuid, fkey a `businesses.id` con `ON DELETE CASCADE`), y `destination_business_id` (uuid, fkey a `businesses.id` con `ON DELETE CASCADE`). Esto previene bloqueos de base de datos al eliminar negocios asociados.
- [ ] **5.1.2. Actualizar Tipado en el Proyecto**: Modificar el archivo `src/types/supabase.ts` para declarar los nuevos campos nullable en el tipo de la tabla `marketing_banners` de Supabase (sin usar `any` en las propiedades).
- [ ] **5.1.3. Estandarizar Ciudades Existentes (SQL Migration Script)**: Ejecutar un script `UPDATE` por única vez en Supabase para formatear el campo `city` de todos los registros en `businesses` al formato estandarizado oficial (Ej: `"Cali"` o `"cali"` -> `"Cali, Valle del Cauca, Colombia"`).
- [ ] **5.1.4. Sistema de Tracking de Clics (Base de Datos)**: Añadir una columna numérica en la tabla `businesses` llamada `cross_traffic_clicks` (entero, por defecto `0`). Crear una función remota SQL (RPC) llamada `increment_cross_traffic(p_business_id)` que sume +1 de manera atómica a esa columna. **Debe definirse como SECURITY DEFINER** para permitir que clientes anónimos del explorador incrementen la métrica sin violar las políticas RLS de lectura/escritura (se usa el prefijo `p_` en el parámetro `p_business_id` para evitar el sombreado de nombres con la columna `id` de la tabla).

#### 📁 subfase 5.2: Archivos de Datos de Configuración
- [ ] **5.2.1. Crear JSON Semillero de Ciudades**: Crear e incorporar el archivo `public/colombia.json` (o en la carpeta `utils/`) con la lista oficial completa de municipios y departamentos colombianos estandarizados para poblar los selectores de forma local y ultrarrápida.

#### ⚙️ subfase 5.3: Componentes e Interfaz del Administrador (Admin Panel)
- [ ] **5.3.1. Crear Componente Autocomplete Reutilizable**: Crear `src/components/admin/shared/Autocomplete.tsx`. Debe ser un componente tipo combobox genérico que acepte filtros locales (para el JSON de ciudades) y búsquedas directas server-side (para los 10,000+ negocios) a Supabase. **Implementar obligatoriamente un Debounce de 300ms** en el campo de búsqueda para evitar consultas masivas e innecesarias a Supabase por cada letra presionada. **Doble Modo**: Diseñar el componente para que funcione en dos modos según las props recibidas: Modo Local (si recibe la prop estática `options` para filtrar el JSON de ciudades) o Modo Remoto (si recibe la prop asíncrona `onAsyncSearch` para consultar negocios a Supabase con debounce automático).
- [ ] **5.3.2. Crear Selector de Alcance Desacoplado**: Crear `src/components/admin/marketing/BannerScopeSelector.tsx` aislando la segmentación. **Especificación UX:** Debe implementarse visualmente con 3 Pestañas (Tabs) o Botones de Selección ("🌍 Global", "🏙️ Por Ciudad", "🏠 Por Negocio"). El Autocomplete respectivo solo debe mostrarse al tener su pestaña activa, manteniendo el diseño del modal limpio y premium.
- [ ] **5.3.3. Modificar Modal de Carga de Banners (Interruptor de Enlace)**: Integrar `BannerScopeSelector.tsx` dentro de `src/components/admin/marketing/BannerUploadModal.tsx`. Para el destino, implementar un interruptor visual (Pestañas/Switch) donde el admin elija entre "🔗 Enlace Web Libre" o "🏠 Negocio de Fowy" (mostrando un campo y ocultando el otro). Adaptar la llamada a `onSave` para pasar los campos `target_city`, `target_business_id`, y `destination_business_id`. **Solución Automática**: Si se selecciona "Negocio de Fowy", el sistema guardará el UUID en `destination_business_id` y de forma automática e invisible rellenará el campo obligatorio `link_url` con la ruta del negocio basada en su slug (ej. `"/nombre-del-negocio"`).
- [ ] **5.3.4. Adaptar Hook de Marketing en el Administrador (Compatibilidad)**: Extender el hook `src/hooks/useMarketingManager.ts` (`addBanner` y `updateBanner`) para aceptar opcionalmente los nuevos parámetros de alcance (`target_city`, `target_business_id`, y `destination_business_id`), garantizando compatibilidad absoluta hacia atrás con las llamadas antiguas y la lógica de CTAs.
- [ ] **5.3.5. Automatizar Ubicación con Pin del Mapa (Zero-Click UX & Fallbacks)**: 
    *   **Creación limpia**: Quitar los campos de ubicación en `AddBusinessModal.tsx` (se crean como vacíos/null; mostrar badge de "⚠️ Ubicación Pendiente" en el panel).
    *   **Geolocalización en Edición**: En `LocationPicker.tsx`, al mover el pin, usar Nominatim OSM.
    *   **Fallback por fallo**: Si la API de OSM falla, habilitar el buscador manual `Autocomplete` de respaldo.
    *   **Fuzzy Matching**: Programar un buscador inteligente que traduzca las respuestas de OSM (ej: "Santiago de Cali" o "Cali") al formato exacto de `colombia.json` ("Cali, Valle del Cauca, Colombia").
- [ ] **5.3.6. Agregar Métrica de Tráfico Cruzado**: Modificar `src/components/admin/businesses/BusinessMetricsList.tsx` para realizar una consulta a Supabase que recupere la columna `cross_traffic_clicks` de la tabla `businesses` para el `businessId` actual, e inyectar un nuevo ítem en la lista de métricas llamado "Clics de Tráfico Cruzado" con un ícono representativo.

#### 📱 subfase 5.4: Lógica de Negocio y Frontend en el Explorador
- [ ] **5.4.1. Instalar Dependencia SWR**: Ejecutar en la terminal el comando `npm install swr` para añadir la librería necesaria para el manejo eficiente de la caché de red.
- [ ] **5.4.2. Crear Hook useSegmentedBanners con Caché (SWR)**: Crear un hook nuevo `src/hooks/useSegmentedBanners.ts` aplicando la Ley del Remolque. Usar `swr` para la caché de red. **Especificaciones de Consulta**:
    *   **Filtro OR seguro (Construcción Dinámica)**: Estructurar la query a Supabase para recuperar banners globales, de la ciudad o del negocio en una sola llamada. **Cuidado con la sintaxis PostgREST**: El string del `.or()` debe construirse dinámicamente en JS (usando condicionales) para evitar errores. Si la variable `city` llega vacía/nula, no intentes inyectarla porque `.eq.null` romperá la base de datos (se usa `.is.null`). Además, si `city` tiene valor, debe ir envuelto obligatoriamente en comillas dobles (ej: `target_city.eq."${city}"`) para que las comas del nombre de la ciudad no rompan el formato de la consulta.
    *   **Filtro en JavaScript**: El filtrado de banners con destino inactivo debe hacerse en JavaScript dentro del hook (y no por INNER JOIN en Supabase) para evitar ocultar de forma accidental los banners que tengan enlaces web libres (`destination_business_id = null`).
    *   **Consulta Relacional (Join para el Slug)**: En la petición a Supabase, se debe incluir una relación (`join`) para traer también el `slug` del negocio de destino y su estado **`status`** (ya que la columna en base de datos es `status` y no `active`). Tener solo el UUID (`destination_business_id`) no sirve para redireccionar; se necesita obligatoriamente el `slug` para que el código luego pueda armar la URL y ejecutar `router.push('/nombre-del-negocio')`.
- [ ] **5.4.3. Pasar Contexto del Negocio desde page.tsx**: Actualizar la llamada a `<AutoScrollBanners />` en `src/app/(explorer)/[slug]/page.tsx` para suministrarle los parámetros de contexto: `<AutoScrollBanners businessId={business.id} city={business.city} />` (sin tocar el fetching interno del orquestador legacy).
- [ ] **5.4.4. Implementar Firma y Props en AutoScrollBanners**: Modificar `src/components/explorer/AutoScrollBanners.tsx` para que reciba las propiedades `businessId` y `city`, y consuma los banners segmentados del hook `useSegmentedBanners` en lugar de `useActiveBanners`.
- [ ] **5.4.5. Incorporar Interacción de Swipe Manual (Pausa táctil inteligente)**: Modificar la animación en `AutoScrollBanners.tsx` para que, cuando el usuario toque la pantalla para ver un banner o deslizarlo con el dedo, el movimiento se detenga al instante en esa posición exacta. Al soltarlo, la app debe esperar 3 segundos y volver a arrancar el movimiento suavemente. **Implementación fluida**: Controlar el scroll continuo infinito usando Framer Motion en lugar de animaciones CSS (`@keyframes`) para evitar saltos bruscos (glitches) en la transición de movimiento automático a arrastre manual. **Velocidad Constante**: Al reanudar el movimiento, se debe calcular dinámicamente el tiempo restante de la animación en base a la coordenada `x` de liberación del usuario (usando `Tiempo restante = Distancia restante / Velocidad constante`) para evitar aceleraciones o desaceleraciones bruscas del carrusel.
- [ ] **5.4.6. Carga Dinámica (Component-level Lazy Loading)**: Importar el componente `<AutoScrollBanners />` en `src/app/(explorer)/[slug]/page.tsx` usando `next/dynamic` con `ssr: false` para evitar que el bundle se descargue en el render inicial de la página.
- [ ] **5.4.7. Carga bajo demanda (Lazy Wrapper)**: Envolver `<AutoScrollBanners />` en el componente `<LazyWrapper />` dentro de `page.tsx` para asegurar que el fetching a la base de datos de Supabase solo se dispare cuando el usuario haga scroll y el banner sea visible en pantalla.
- [ ] **5.4.8. Rastrear Clics de Banners (Sin Bloqueos)**: Modificar el evento de clic en `AutoScrollBanners.tsx`. Si el banner tiene un `destination_business_id`, disparar la función RPC `increment_cross_traffic` en segundo plano (sin usar `await`) y acto seguido ejecutar la redirección (`router.push`) para que el usuario no sienta ningún retraso (lag).

